The time complexity of the makeEmpty(BinaryNode* &t) function is O(n).
This is because the function visits and deletes every node exactly once regardless
of the size of the subtree rooted at the node.